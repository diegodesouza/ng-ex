/**
 *  Alphabet Carousel item interface. Contains values to emit
 *  to parent container.
 *
 *  @param String { label } Alphabet range meant to serve as static label string
 *  @param String { value } Alphabet range in value form
 */
export interface ICarouselItem {
  label: string;
  value: string;
}
